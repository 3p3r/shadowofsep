/**
 * Central color definitions for the portfolio.
 */
export const palette = {
  /** Full-page background (Phaser canvas matches this). */
  background: "#002956",
} as const;

export type Palette = typeof palette;
